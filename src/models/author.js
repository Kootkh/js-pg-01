// const mongoose = require('mongoose')
const {
    Pool
} = require('pg')

const pool = new Pool()

// const authorSchema = new pg

// const authorSchema = new mongoose.Schema({
//     name: {
//         type: String,
//         required: true
//     }
// })

// module.exports = mongoose.model('Author', authorSchema)